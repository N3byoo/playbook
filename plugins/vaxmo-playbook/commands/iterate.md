---
name: iterate
description: Run a full Expo iteration cycle (8 phases)
argument-hint: <what to improve>
---

# /iterate — Full Expo Iteration Cycle

You have been asked to run a full iteration cycle. Follow the **expo-iterate** skill exactly — all 8 phases, no skipping.

## What to do

1. Invoke the `expo-iterate` skill
2. The argument passed to this command is the user's request for what to update/improve
3. If no argument was given, ask the user what they want to work on this cycle
4. Follow all 8 phases: Understand → Enrich → Plan → Execute → Verify → Deploy → Retrospective → Report
5. Do not stop until the APK link is delivered and the backlog is updated
